server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location ^~ /mock-scenes/ {
        return 410;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
